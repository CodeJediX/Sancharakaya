window.SANCHARAKAYA_CONFIG = {
  // The embedded assistant in the main site talks to this local Node API.
  // For deployment, replace it with your hosted backend URL.
  API_BASE_URL: "http://localhost:8787"
};
